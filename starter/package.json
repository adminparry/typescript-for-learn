{
  "name": "starter",
  "version": "1.0.0",
  "description": "",
  "main": "dist/base.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist/ && cd src/ && tsc",
    "dev": "cross-env NODE_ENV=development nodemon --watch scripts --watch dist dist/scripts/base.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/koa": "^2.0.48",
    "@types/koa-router": "^7.0.40",
    "@types/node": "^11.13.8",
    "cross-env": "^5.2.0",
    "koa": "^2.7.0",
    "koa-body": "^4.1.0",
    "koa-router": "^7.4.0"
  }
}
